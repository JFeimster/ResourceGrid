import Link from "next/link";import { marketplaceRegistry,recommendMarketplace,searchMarketplace } from "../lib/marketplace";export const dynamic="force-dynamic";export default async function MarketplacePage({searchParams}:{searchParams:Promise<{q?:string;theme?:string;kind?:string;platform?:string;goal?:string}>}){const params=await searchParams,registry=marketplaceRegistry(),results=searchMarketplace(params),recs=params.goal?recommendMarketplace(params.goal,6):[];return <main className="marketplaceShell"><section className="marketplaceHero"><p className="eyebrow">ONE REGISTRY → MANY RUNTIMES</p><h1>Operator Marketplace</h1><p className="lede">Installable skills, tools, and Operator Packs generated from one canonical capability registry for OpenAI, Claude, Codex, MCP, APIs, VS Code, and SaaS products.</p><div className="marketStats"><span><strong>{registry.listings.length}</strong> capabilities</span><span><strong>{registry.themes.length}</strong> themes</span><span><strong>{registry.adapterTargets.length}</strong> runtimes</span></div></section><section className="marketFilters"><form action="/marketplace" method="get"><input name="goal" defaultValue={params.goal??""} placeholder="Tell us what you want to accomplish…" aria-label="Goal"/><button type="submit">Recommend stack</button></form></section>{params.goal&&<section className="marketGrid">{recs.map(x=><Link key={x.listing.slug} href={`/marketplace/${x.listing.slug}`} className="marketCard"><div className="marketCardTop"><span>{x.listing.theme}</span><b>{x.score} pts</b></div><h2>{x.listing.name}</h2><p>{x.listing.tagline}</p><div className="marketTags">{x.reasons.map(r=><span key={r}>{r}</span>)}</div></Link>)}</section>}<form className="marketFilters" action="/marketplace" method="get"><input name="q" defaultValue={params.q??""} placeholder="Search capabilities, jobs, or outcomes…" aria-label="Search marketplace"/><select name="theme" defaultValue={params.theme??""}><option value="">All themes</option>{registry.themes.map(theme=><option key={theme} value={theme}>{theme}</option>)}</select><select name="kind" defaultValue={params.kind??""}><option value="">All types</option><option value="operator">Operators</option><option value="skill">Skills</option><option value="tool">Tools</option></select><select name="platform" defaultValue={params.platform??""}><option value="">All platforms</option>{registry.adapterTargets.map(t=><option key={t.id} value={t.id}>{t.label}</option>)}</select><button type="submit">Search</button></form><div className="marketMeta"><span>{results.length} results</span><Link href="/api/v1/plugins">JSON API</Link><Link href="/api/v1/openapi">OpenAPI</Link><Link href="/api/mcp">MCP</Link></div><section className="marketGrid">{results.map(listing=><Link href={`/marketplace/${listing.slug}`} key={listing.id} className="marketCard"><div className="marketCardTop"><span>{listing.theme}</span><b>{listing.kind}</b></div><h2>{listing.name}</h2><p>{listing.tagline}</p><div className="marketTags">{listing.platforms.slice(0,5).map(p=><span key={p}>{p}</span>)}</div><div className="marketCardFoot"><span>{listing.instructionStatus??listing.availability}</span><span>v{listing.version} →</span></div></Link>)}</section></main>;}
