import { NextResponse } from "next/server";import { getMarketplaceListing,getMarketplacePackageStatus } from "../../../../lib/marketplace";export async function GET(_request:Request,{params}:{params:Promise<{slug:string}>}){const{slug}=await params,listing=getMarketplaceListing(slug);if(!listing)return NextResponse.json({error:"plugin_not_found"},{status:404});return NextResponse.json({...listing,package:getMarketplacePackageStatus(slug)});}
