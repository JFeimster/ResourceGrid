import { NextRequest,NextResponse } from "next/server";import { buildAdapter,getMarketplaceListing,type AdapterPlatform } from "../../../../../lib/marketplace";
const platforms=new Set<AdapterPlatform>(["openai","claude","codex","mcp","api","vscode","saas"]);
export async function GET(request:NextRequest,{params}:{params:Promise<{platform:string;slug:string}>}){const{platform,slug}=await params;if(!platforms.has(platform as AdapterPlatform))return NextResponse.json({error:"unsupported_platform"},{status:400});const listing=getMarketplaceListing(slug);if(!listing)return NextResponse.json({error:"plugin_not_found"},{status:404});if(!listing.platforms.includes(platform as AdapterPlatform))return NextResponse.json({error:"package_not_available"},{status:409});const payload=buildAdapter(platform as AdapterPlatform,listing,new URL(request.url).origin) as any;const requestedFile=request.nextUrl.searchParams.get("file");if(requestedFile){const files=Array.isArray(payload.files)?payload.files:[],file=files.find((x:{path?:string})=>x.path===requestedFile);if(!file)return NextResponse.json({error:"package_file_not_found"},{status:404});const type=requestedFile.endsWith(".json")?"application/json; charset=utf-8":"text/markdown; charset=utf-8",disp=request.nextUrl.searchParams.get("download")==="1"?"attachment":"inline";return new NextResponse(file.content,{headers:{"Content-Type":type,"Content-Disposition":`${disp}; filename="${requestedFile.split("/").pop()}"`,"Cache-Control":"public, max-age=300, s-maxage=300"}});}return NextResponse.json(payload,{headers:{"Cache-Control":"public, max-age=300, s-maxage=300"}});}
