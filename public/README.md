# ResourceGrid Public Assets

Static assets safe for public deployment.

Structure:
- `brand/` logos and brand marks
- `icons/` favicon/app icons
- `images/` public UI and Open Graph images

Do not place private registries, CRM/applicant data, Notion/Drive exports, secrets, or internal-only JSON here. Everything in `public/` is directly web-accessible.
